# 总览与验证记录

## 一、用途

`总览.md` 记录当前 locate 会话的阶段快照。`验证记录.md` 记录 sink、上游依赖或分叉假设的证明过程。

请求结构、字段来源和上游展开继续写在 `请求链路.md`，不要挪到 `总览.md` 或 `验证记录.md`。

## 二、落盘路径

默认写到当前任务工作目录：

```text
reverse-records/
├─ 会话1/
│  ├─ 总览.md
│  ├─ 请求链路.md
│  ├─ 运行时依赖.md
│  ├─ 恢复记录.md
│  └─ 验证记录.md
├─ 会话2/
│  └─ ...
└─ ...
```

## 三、总览骨架

```markdown
# 总览

## 目标定义
- 目标请求：
- 目标字段：
- 最终 sink：
- 触发动作：
- 当前状态：

## 当前阶段
- 样本采集 / 写边界定位 / 上游展开 / 风控分叉核对

## 已确认
- ...

## 当前卡点
- ...

## 下一步
- ...

## 风险
- ...

## 待验证
- ...

## 当前状态块
- status：`ready / partial / blocked`
- stage：`locate`
- code：
- summary：
- evidence：
- impact：
- next_action：

## 正常态 / 风控态对比（按需）
| Item | 正常态 | 风控态 | Same chain |
|---|---|---|---|
| 触发动作 |  |  |  |
| 上游请求 |  |  |  |
| 写点 |  |  |  |
| builder / writer |  |  |  |
| Cookie / 状态依赖 |  |  |  |

## 正常态 / 风控态分叉图（按需）
- 分叉起点：
- 正常态路径：
- 风控态路径：
- 缺失状态：
```

## 四、验证骨架

```markdown
# 验证记录

## 验证项1
- 验证目标：
- 所属阶段：locate

### 固定输入
- 输入样本：
- 会话状态：
- 页面动作：

### 检查点
- 检查点1：
- 检查点2：
- 检查点3：

### 结果
- 是否通过：
- 仍有缺口：
- 证据：
```

## 五、路由规则

- `总览.md` 负责 `当前阶段 / 已确认 / 当前卡点 / 下一步 / 风险 / 待验证` 和当前规范化状态块。
- 正常态 / 风控态对比表和分叉图只能放在 `总览.md`，不能写进 `请求链路.md`。
- `请求链路.md` 负责请求区块、状态数组、`来源/去向`、证据和上游展开。
- `验证记录.md` 在 sink 假设、状态闭合或分叉假设需要证明时开始记录。

## 六、检查标准

- `总览.md` 含有六个固定阶段字段。
- 只要阶段处于 `partial / blocked`，`总览.md` 就必须填完整的当前状态块。
- 只有在存在风控分支时才写分叉图。
- `验证记录.md` 使用具体检查点，而不是只有最终结论。
- 请求结构内容不混进 `总览.md`。
